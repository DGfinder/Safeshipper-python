# Security scanning requirements for SafeShipper
bandit>=1.7.5
safety>=3.0.0
semgrep>=1.50.0
gitpython>=3.1.40
requests>=2.31.0
jinja2>=3.1.2
pyyaml>=6.0.1
click>=8.1.0

# Optional: Advanced security tools (install separately)
# trivy (via package manager)
# gitleaks (via GitHub releases)
# codeql (via GitHub CLI)
# snyk (via npm)
# checkov (via pip install checkov)

# Reporting and visualization
matplotlib>=3.7.0
plotly>=5.17.0

# CI/CD integration
python-gitlab>=4.0.0
pygithub>=1.59.0